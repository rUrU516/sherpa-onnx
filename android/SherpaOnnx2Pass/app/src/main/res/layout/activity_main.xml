<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    tools:context=".MainActivity">

    <LinearLayout
        android:id="@+id/status_bar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:paddingStart="12dp"
        android:paddingTop="8dp"
        android:paddingEnd="12dp"
        android:paddingBottom="8dp"
        android:background="#111111"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <FrameLayout
            android:id="@+id/level_track"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_marginBottom="8dp"
            android:background="#222222">

            <View
                android:id="@+id/level_fill"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:background="#FFFFFF" />
        </FrameLayout>

        <TextView
            android:id="@+id/tv_online_decode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            android:text="DECODE: 0"
            android:textColor="#666666"
            android:fontFamily="monospace"
            android:typeface="monospace"
            android:textSize="12sp"
            android:letterSpacing="0.05" />

        <TextView
            android:id="@+id/tv_loop_usage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            android:text="LOOP: 0%"
            android:textColor="#666666"
            android:fontFamily="monospace"
            android:typeface="monospace"
            android:textSize="12sp"
            android:letterSpacing="0.05" />

        <TextView
            android:id="@+id/tv_endpoint_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            android:text="ENDPOINT: NO"
            android:textColor="#666666"
            android:fontFamily="monospace"
            android:typeface="monospace"
            android:textSize="12sp"
            android:letterSpacing="0.05" />

        <TextView
            android:id="@+id/tv_offline_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="OFFLINE: IDLE"
            android:textColor="#666666"
            android:fontFamily="monospace"
            android:typeface="monospace"
            android:textSize="12sp"
            android:letterSpacing="0.05" />
    </LinearLayout>

    <!-- ScrollView Container -->
    <ScrollView
        android:id="@+id/scroll_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        android:scrollbars="vertical"
        android:fadeScrollbars="false"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/text_bg"
        app:layout_constraintTop_toBottomOf="@id/status_bar"
        app:layout_constraintBottom_toTopOf="@id/record_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Text Display Area: Terminal Style -->
        <TextView
            android:id="@+id/my_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:singleLine="false"
            android:text="@string/hint"
            android:textColor="#DDDDDD"
            android:fontFamily="monospace"
            android:typeface="monospace"
            android:gravity="top|start"
            android:lineSpacingExtra="4dp"
            android:textSize="14sp"
            android:padding="16dp" />
    </ScrollView>

    <Button
        android:id="@+id/btn_offline_model"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="8dp"
        android:text="OFFLINE MODEL: SENSEVOICE"
        android:textAllCaps="true"
        android:textColor="#FFFFFF"
        android:background="@drawable/btn_outline"
        android:fontFamily="monospace"
        android:typeface="monospace"
        android:textSize="12sp"
        android:letterSpacing="0.05"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        app:backgroundTint="@null"
        app:layout_constraintBottom_toTopOf="@id/record_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Big Button at the Bottom: Web Style -->
    <Button
        android:id="@+id/record_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp"
        android:text="@string/start"
        android:textAllCaps="true"
        android:textColor="#FFFFFF"
        android:background="@drawable/btn_outline"
        android:fontFamily="monospace"
        android:typeface="monospace"
        android:textSize="16sp"
        android:letterSpacing="0.15"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        app:backgroundTint="@null"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <FrameLayout
        android:id="@+id/switching_overlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#AA000000"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tv_switching_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="SWITCHING OFFLINE MODEL..."
            android:textColor="#FFFFFF"
            android:fontFamily="monospace"
            android:typeface="monospace"
            android:textSize="16sp"
            android:letterSpacing="0.08" />
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
